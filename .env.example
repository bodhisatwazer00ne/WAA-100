DATABASE_URL="postgresql://postgres:postgres@localhost:5432/waa100?schema=public"

JWT_SECRET="change-this-in-production"
JWT_EXPIRES_IN="7d"

# Preferred on Render Free: Resend HTTP API
RESEND_API_KEY=""
# Use a verified sender/domain in Resend.
# For initial testing in your own account, onboarding@resend.dev may work.
RESEND_FROM="WAA-100 <onboarding@resend.dev>"
RESEND_API_BASE_URL="https://api.resend.com"

# Optional SMTP fallback (used only if RESEND_API_KEY is empty)
SMTP_URL=""
SMTP_SERVICE=""
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_REQUIRE_TLS="true"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="WAA-100 <no-reply@example.com>"

APP_BASE_URL="http://localhost:5173"
BACKEND_PORT="4000"

# Frontend build-time API base URL (for deployed static site)
# Example: https://waa-100-backend.onrender.com
VITE_API_BASE_URL=""
