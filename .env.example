DATABASE_URL="postgresql://postgres:postgres@localhost:5432/waa100?schema=public"

JWT_SECRET="change-this-in-production"
JWT_EXPIRES_IN="7d"

# Preferred on Render Free: Mailgun HTTP API (sandbox-friendly)
MAILGUN_API_KEY=""
# Example sandbox domain: sandbox-xxxx.mailgun.org
MAILGUN_DOMAIN=""
# Example sender for sandbox: WAA-100 <postmaster@sandbox-xxxx.mailgun.org>
MAILGUN_FROM=""
MAILGUN_API_BASE_URL="https://api.mailgun.net"

# Optional SMTP fallback (used only if MAILGUN_API_KEY/MAILGUN_DOMAIN are empty)
SMTP_URL=""
SMTP_SERVICE=""
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_REQUIRE_TLS="true"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="WAA-100 <no-reply@example.com>"

APP_BASE_URL="http://localhost:5173"
BACKEND_PORT="4000"

# Frontend build-time API base URL (for deployed static site)
# Example: https://waa-100-backend.onrender.com
VITE_API_BASE_URL=""
